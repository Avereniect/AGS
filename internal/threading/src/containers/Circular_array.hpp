#ifndef AGS_CIRCULAR_ARRAY_HPP
#define AGS_CIRCULAR_ARRAY_HPP

#include <atomic>
#include <mutex>

namespace ags::threading {

    template<class T>
    class Circular_array {
    public:

        //=================================================
        // -ctors
        //=================================================

        //=================================================
        // Assignment operators
        //=================================================

    private:

    };

}

#endif //AGS_CIRCULAR_ARRAY_HPP
